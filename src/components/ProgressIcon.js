import React from 'react'
import { View } from 'react-native'

import { Entypo } from '@expo/vector-icons'

const ProgressIcon = () => {
    return (
        <Entypo name={"progress-full"} size={50} color={'black'}/>
    )
}

export default ProgressIcon